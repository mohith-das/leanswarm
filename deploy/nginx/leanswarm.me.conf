# HTTP → HTTPS redirect. certbot --nginx will add the HTTPS server block
# with cert paths when setup.sh runs it.
server {
    listen 80;
    listen [::]:80;
    server_name leanswarm.me;

    return 301 https://$host$request_uri;
}
